repos:
  # Go formatting and linting (using local hooks for better compatibility)
  - repo: local
    hooks:
      - id: go-fmt
        name: Format Go code
        entry: go fmt ./...
        language: system
        pass_filenames: false
        types: [go]
      - id: go-vet
        name: Run go vet
        entry: go vet ./...
        language: system
        pass_filenames: false
        types: [go]
      - id: go-mod-tidy
        name: Tidy Go modules
        entry: bash -c 'go mod tidy && git diff --exit-code go.mod go.sum'
        language: system
        pass_filenames: false
        types: [go]

  # Golangci-lint (using system-installed version for Go 1.25+ compatibility)
  - repo: local
    hooks:
      - id: golangci-lint
        name: Run golangci-lint
        entry: golangci-lint run
        language: system
        pass_filenames: false
        types: [go]

  # General file checks
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v4.5.0
    hooks:
      - id: check-added-large-files
        name: Check for large files
        args: ['--maxkb=500']
      - id: check-case-conflict
        name: Check for case conflicts
      - id: check-merge-conflict
        name: Check for merge conflicts
      - id: check-yaml
        name: Check YAML syntax
        exclude: mkdocs.yml  # mkdocs.yml uses Python-specific YAML tags
      - id: check-json
        name: Check JSON syntax
      - id: check-toml
        name: Check TOML syntax
      - id: end-of-file-fixer
        name: Fix end of files
      - id: trailing-whitespace
        name: Trim trailing whitespace
      - id: mixed-line-ending
        name: Fix mixed line endings
        args: ['--fix=lf']
      - id: detect-private-key
        name: Detect private keys

  # Spell checking
  - repo: https://github.com/codespell-project/codespell
    rev: v2.2.6
    hooks:
      - id: codespell
        name: Check spelling
        args: [
          '--ignore-words-list=crate,Erro',
          '--skip=*.svg,*.png,*.jpg,*.ico,go.sum,*.log'
        ]

  # Markdown lint
  - repo: https://github.com/igorshubovych/markdownlint-cli
    rev: v0.39.0
    hooks:
      - id: markdownlint
        name: Lint Markdown files
        args: ['--fix']

  # License headers (custom hook)
  - repo: local
    hooks:
      - id: go-license-header
        name: Check license headers
        entry: bash -c 'for file in $(git diff --cached --name-only --diff-filter=ACM | grep "\.go$"); do if ! grep -q "Copyright (c)" "$file"; then echo "Missing license header in $file"; exit 1; fi; done || true'
        language: system
        types: [go]
        pass_filenames: false

  # Go tests
  - repo: local
    hooks:
      - id: go-test
        name: Run Go tests
        entry: go test -v -race ./...
        language: system
        pass_filenames: false
        types: [go]

  # Go build check
  - repo: local
    hooks:
      - id: go-build
        name: Build Go application
        entry: go build -o /dev/null ./cmd/gatus-monitor
        language: system
        pass_filenames: false
        types: [go]
